"""UBCHelper — a retrieval-augmented UBC Vancouver Academic Calendar advisor.

A domain re-skin of RAGChef: the same generic retrieval/generation engine over a scraped
Academic Calendar corpus. This package currently holds the data layer — scrape -> chunk ->
ingest — which fills the engine's document index.
"""
